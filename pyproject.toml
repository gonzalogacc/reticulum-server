[project]
name = "fast-ret"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "lxmf>=1.0.1",
    "rns>=1.3.5",
    "watchfiles>=1.2.0",
]
